/** UGC Ops FastAPI base URL. Override for production deployments. */
export const UGC_BACKEND_URL = 'http://localhost:8000';

/** Optional; only when backend has INTAKE_API_SECRET set (not for production bundles). */
export const UGC_INTAKE_API_SECRET = '';
